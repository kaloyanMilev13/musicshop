**Findings**
- Critical: Admin-only data exposure; any logged-in user can fetch all orders and full customer PII because admin checks are explicitly skipped. `api/get_all_orders.php:12`
- High: SQL injection risk in login by interpolating user input into SQL; `real_escape_string` is not a substitute for prepared statements. `api/login.php:14` `api/login.php:17`
- High: CSRF protections are missing for state-changing endpoints (favorites, orders, admin CRUD, logout), and at least one destructive action uses GET. Examples: `api/add_favorite.php:1` `api/create_order.php:1` `api/admin_delete_product.php:5` `api/logout.php:1`
- High: Stored XSS risk from rendering DB/user-controlled fields via `innerHTML` without sanitization (products, favorites, order customer fields). `products.html:471` `favorites.html:369` `orders.html:229`
- Medium: Secrets and internal errors are exposed to clients (hardcoded DB creds, raw DB errors in responses). `api/db.php:4` `api/db.php:12` `api/admin_add_product.php:56` `api/admin_add_news.php:25` `api/add_favorite.php:26`
- Medium: Session fixation protection missing; login does not call `session_regenerate_id(true)` and does not set `$_SESSION['email']` per project conventions. `api/login.php:21` `api/login.php:23`
- Low: Public test endpoint leaks DB server/host info; should be removed or admin-protected. `api/test_db.php:1`

Open questions / assumptions
- Are there any existing CSRF helpers or middleware elsewhere in the repo, or should I add a new `_auth.php`/CSRF helper as per `AGENTS.md`?
- Should admin-only endpoints be restricted to `requireAdmin()` even in local/dev (e.g., `get_all_orders.php`)?

Change summary
- No code changes made; review only.

If you want, I can draft a patch to add `requireLogin/requireAdmin`, CSRF tokens, prepared login, and XSS-safe rendering.



To continue this session, run codex resume 019b4a01-8068-7852-bd0c-70b1709140a6

